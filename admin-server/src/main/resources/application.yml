spring:
  profiles:
    active: standalone
  application:
    name: admin-server
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:h2:mem:admin
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
server:
  port: ${ADMIN_SERVER_PORT:8085}
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
